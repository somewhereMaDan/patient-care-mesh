syntax = "proto3";

package patient.events;
option java_multiple_files = true;

message PatientEvent{
  string patientId = 1;
  string name = 2;
  string email = 3;
  string event_type = 4;
}

/*
The protobuf compiler generated the Java code equivalent to:
public final class PatientEvent {
    private String patientId;
    private String name;
    private String email;
    private String eventType;

    // builder, getters, serialization, parsing, etc.
}

.proto file
    │
    │ Maven/protoc generates Java classes
    ↓
PatientEvent.java
    │
    │ runtime
    ↓
PatientEvent.newBuilder()
    │
    ↓
event.toByteArray()
    │
    ↓
KafkaTemplate.send()
    │
    ↓
Kafka topic
*/